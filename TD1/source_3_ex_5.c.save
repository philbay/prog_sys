#include <stdlib.h>
#include <stdio.h>

int main(){

	char *getenv(const char *nomDeVariable);
	int putenv(const char *coupleNomValeur);
	extern char **environ;
	
	for(int i = 0; i < sizeof(environ) - 1; i++)
{
	printf("test + %s\n",  environ[i]);
}
return 0;
}
